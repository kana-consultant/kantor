name: Build & Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      system-path: ${{ steps.build.outputs.path }}
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main

      - uses: cachix/cachix-action@v15
        with:
          name: kantor-kana
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Setup sops age key
        run: |
          mkdir -p ~/.config/sops/age
          echo "${{ secrets.SOPS_AGE_KEY }}" > ~/.config/sops/age/keys.txt

      - name: Clone nix-config
        run: git clone https://oauth2:${{ secrets.GITLAB_TOKEN }}@gitlab.com/KeyCode17/nix-config.git /tmp/nix-config

      - name: Build NixOS system
        id: build
        run: |
          SYSTEM=$(nix build \
            /tmp/nix-config#nixosConfigurations.lp-covertplay.config.system.build.toplevel \
            --override-input kantor . \
            --print-out-paths --no-link)
          echo "path=$SYSTEM" >> "$GITHUB_OUTPUT"

      - name: Push to Cachix
        run: cachix push kana-consultant ${{ steps.build.outputs.path }}

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy-key
          chmod 600 ~/.ssh/deploy-key
          ssh-keyscan -H ${{ secrets.DEPLOY_HOST }} >> ~/.ssh/known_hosts 2>/dev/null
          echo -e "Host *\n  IdentityFile ~/.ssh/deploy-key\n  StrictHostKeyChecking accept-new" > ~/.ssh/config
          chmod 600 ~/.ssh/config

      - name: Deploy to server
        run: |
          ssh root@${{ secrets.DEPLOY_HOST }} "\
            nix-store -r ${{ needs.build.outputs.system-path }} && \
            nix-env -p /nix/var/nix/profiles/system --set ${{ needs.build.outputs.system-path }} && \
            ${{ needs.build.outputs.system-path }}/bin/switch-to-configuration switch"
